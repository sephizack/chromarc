// common.js
// Shared constants for the extension

// Shared constant for default tab timeout (6 hours in ms)
export const DEFAULT_TAB_TIMEOUT = 6 * 60 * 60 * 1000;

// // if modules are supported, export the constant
// if (typeof module !== 'undefined' && module.exports) {
//     module.exports = { DEFAULT_TAB_TIMEOUT };
// }
